plugin {
    scroller {
        column_default_width = onehalf
        focus_wrap = false
        # ultra-wide monitor
        #column_widths = onefourth onethird onehalf onesixth
        column_widths = maximized
    }
}
